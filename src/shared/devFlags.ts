// 开发期是否默认打开 Electron DevTools。
// `true`：每次启动桌面端都会自动弹出 DevTools。
// `false`：保持关闭，需要时再手动开启。
export const OPEN_DEVTOOLS = false;

// 开发期是否输出桌面端交互诊断日志。
// `true`：输出 preload 注入、宠物点击/拖拽、面板开关等调试日志。
// `false`：关闭这些排障日志，终端会更干净。
export const ENABLE_DESKTOP_DEBUG_LOGS = false;
